import 'package:flutter/cupertino.dart';

class SearchNewsScreen extends StatelessWidget {
  const SearchNewsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('SearchNews')
    );
  }
}
